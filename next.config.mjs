/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["us-west-2.graphassets.com", "res.cloudinary.com"], // Agrega el dominio aquí
  },
};

export default nextConfig;
